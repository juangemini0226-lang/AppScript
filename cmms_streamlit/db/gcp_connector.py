""" db/gcp_connector.py ==================== Implementación de DBConnector para Google Cloud SQL (PostgreSQL), usando el Cloud SQL Python Connector + SQLAlchemy como capa SQL. """ from __future__ import annotations from typing import Any, Optional import sqlalchemy from google.cloud.sql.connector import Connector, IPTypes from google.oauth2 import service_account from db.base import DBConnector class GCPCloudSQLConnector(DBConnector): def __init__(self, settings: dict): self.settings = settings self._connector: Optional[Connector] = None self._engine: Optional[sqlalchemy.engine.Engine] = None def connect(self) -> None: if self._engine is not None: return creds_json = self.settings.get("credentials_json") credentials = None if creds_json: credentials = service_account.Credentials.from_service_account_info( dict(creds_json), scopes=["https://www.googleapis.com/auth/sqlservice.admin"], ) ip_type = IPTypes.PRIVATE if self.settings.get("ip_type") == "PRIVATE" else IPTypes.PUBLIC self._connector = Connector(ip_type=ip_type, credentials=credentials) def getconn(): return self._connector.connect( self.settings["instance_connection_name"], "pg8000", user=self.settings["db_user"], password=self.settings["db_pass"], db=self.settings["db_name"], ) self._engine = sqlalchemy.create_engine( "postgresql+pg8000://", creator=getconn, pool_size=5, max_overflow=2, pool_timeout=30, pool_recycle=1800, ) def close(self) -> None: if self._engine is not None: self._engine.dispose() self._engine = None if self._connector is not None: self._connector.close() self._connector = None def _ensure_conn(self): if self._engine is None: self.connect() return self._engine @staticmethod def _where_clause(where): if not where: return "", {} parts = [f"{k} = :w_{k}" for k in where.keys()] params = {f"w_{k}": v for k, v in where.items()} return " WHERE " + " AND ".join(parts), params def fetch_all(self, table, where=None, order_by=None, limit=None): engine = self._ensure_conn() where_sql, params = self._where_clause(where) query = f"SELECT * FROM {table}{where_sql}" if order_by: query += f" ORDER BY {order_by}" if limit: query += f" LIMIT {int(limit)}" with engine.connect() as conn: result = conn.execute(sqlalchemy.text(query), params) return [dict(row._mapping) for row in result] def fetch_one(self, table, where): rows = self.fetch_all(table, where=where, limit=1) return rows[0] if rows else None def insert(self, table, data): engine = self._ensure_conn() cols = ", ".join(data.keys()) placeholders = ", ".join(f":{k}" for k in data.keys()) query = f"INSERT INTO {table} ({cols}) VALUES ({placeholders}) RETURNING *" with engine.begin() as conn: result = conn.execute(sqlalchemy.text(query), data) row = result.fetchone() return dict(row._mapping) if row else None def update(self, table, where, data): engine = self._ensure_conn() set_sql = ", ".join(f"{k} = :set_{k}" for k in data.keys()) where_sql, where_params = self._where_clause(where) params = {f"set_{k}": v for k, v in data.items()} params.update(where_params) query = f"UPDATE {table} SET {set_sql}{where_sql}" with engine.begin() as conn: result = conn.execute(sqlalchemy.text(query), params) return result.rowcount def delete(self, table, where): engine = self._ensure_conn() where_sql, params = self._where_clause(where) query = f"DELETE FROM {table}{where_sql}" with engine.begin() as conn: result = conn.execute(sqlalchemy.text(query), params) return result.rowcount def execute_raw(self, query, params=None): engine = self._ensure_conn() with engine.connect() as conn: result = conn.execute(sqlalchemy.text(query), params or {}) if result.returns_rows: return [dict(row._mapping) for row in result] return []
